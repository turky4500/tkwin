databases:
  - name: tkwin-db
    plan: free

services:
  - type: web
    name: tkwin-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tkwin-db
          property: connectionString
